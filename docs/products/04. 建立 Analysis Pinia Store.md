你现在负责 data-ai-analyst 项目的第 4 个小任务。

目标：
建立 Analysis Pinia Store。

请先阅读：
- AGENTS.md
- CONTEXT.md
- Analysis Domain
- analysisService
- 当前 Pinia store

架构：

View
→ Analysis Store
→ analysisService
→ Mock Domain

建立：

frontend/src/stores/analysis.js

Store 至少管理：

- currentAnalysis
- analysisId
- question
- context
- status
- steps
- currentStep
- result
- insights
- recommendations
- drillDownPath
- evidence
- trace
- reportId

Actions：

- createAnalysis()
- loadAnalysis(id)
- runAnalysis()
- retryAnalysis()
- cancelAnalysis()
- loadResult()
- drillDown()
- loadEvidence()
- loadTrace()
- generateReport()

要求：

1. View 不直接调用 analysisService。
2. Store 不操作 DOM。
3. Store 不包含 ECharts 逻辑。
4. Store 不包含 UI drawer 状态。
5. UI 状态例如 drawerOpen、selectedTab 不进入 Store。
6. Analysis 状态必须可以持久化。
7. 可以使用 localStorage。
8. 页面刷新后能够恢复当前 Analysis。
9. 多个 Analysis 之间必须通过 analysisId 区分。
10. 不引入真实 API。

不要修改 HomeView。
不要修改 AnalysisView。
不要修改 Platform 页面。

完成：
- npm run build
- 如果有测试则运行相关测试
- 检查 store 是否可以独立运行
- 总结修改文件。

不要进行无关重构。